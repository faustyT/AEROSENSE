# ============================================================
#  AeroSense — UAV PM2.5 Prediction System
#  Python Dependencies
# ============================================================
#
#  Install all dependencies:
#      pip install -r requirements.txt
#
#  TensorFlow is optional. If not installed, the app uses a
#  102-dim histogram fallback for CNN feature extraction.
#  All other functionality remains identical.
# ============================================================


# ── Core web framework ────────────────────────────────────────
streamlit>=1.28.0


# ── Data science ──────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0


# ── Image processing ──────────────────────────────────────────
Pillow>=10.0.0


# ── Visualisation ─────────────────────────────────────────────
plotly>=5.17.0


# ── Deep learning (optional but recommended) ─────────────────
#
#  Choose ONE of the following depending on your hardware:
#
#  GPU machine (NVIDIA CUDA):
#      tensorflow>=2.13.0
#
#  CPU only (lighter install, slower inference):
#      tensorflow-cpu>=2.13.0
#
#  Apple Silicon (M1/M2/M3):
#      tensorflow-macos>=2.13.0
#
#  Uncomment the line that matches your machine:

tensorflow>=2.13.0
# tensorflow-cpu>=2.13.0
# tensorflow-macos>=2.13.0


# ── Standard library (no install needed) ─────────────────────
#  json, logging, os, pickle, sys, time, traceback,
#  warnings, dataclasses, datetime, pathlib, typing
#  — all included with Python 3.8+